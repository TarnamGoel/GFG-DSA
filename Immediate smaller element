class Solution:

	def immediateSmaller(self,arr,n):
		# code here
        for i in range(len(arr)-1):
            if arr[i]>arr[i+1]:
                arr[i]=arr[i+1]
            else:
                arr[i]=-1
        arr[-1]=-1
        return arr
